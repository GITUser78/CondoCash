// ---------------------------------------------------------------------------
// CondoCash configuration
//
// Fill these in with the values from your Supabase project:
//   Supabase Dashboard → Project Settings → API
//     • Project URL   → SUPABASE_URL
//     • anon / public key → SUPABASE_ANON_KEY
//
// The anon key is safe to expose in the browser: all access is enforced by
// Row Level Security policies in the database (see supabase/schema.sql).
//
// Copy this file to js/config.js (which is git-ignored) and fill in the values.
// ---------------------------------------------------------------------------

export const SUPABASE_URL = "YOUR_SUPABASE_PROJECT_URL";
export const SUPABASE_ANON_KEY = "YOUR_SUPABASE_ANON_KEY";

// Default UI language before a user has chosen one: "bg" or "en".
export const DEFAULT_LANGUAGE = "bg";
